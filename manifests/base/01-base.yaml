apiVersion: apps/v1
kind: Deployment
metadata:
  name: base-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: base-app
  template:
    metadata:
      labels:
        app: base-app
    spec:
      containers:
      - name: base-app
        image: nginx:1.4.9